#include <stdio.h>

int main() {
    char ch;
    char str[100];
    char sentence[100];

    scanf(" %c", &ch);
    scanf("%s", str);
    scanf(" %[^\n]", sentence);

    printf("%c\n%s\n%s\n", ch, str, sentence);
    return 0;
}
